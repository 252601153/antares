# Antares SQL Client 项目结构说明

## 📖 项目概述

**Antares SQL Client** 是一个基于 [Electron.js](https://github.com/electron/electron) 和 [Vue.js](https://github.com/vuejs/vue) 构建的现代化、跨平台 SQL 数据库客户端。

### 核心技术栈

| 技术       | 版本   | 用途                  |
| ---------- | ------ | --------------------- |
| Electron   | 30.x   | 跨平台桌面应用框架    |
| Vue.js     | 3.4.x  | 前端 UI 框架          |
| TypeScript | 4.6.x  | 类型安全的 JavaScript |
| Pinia      | 2.1.x  | Vue 状态管理          |
| Webpack    | 5.x    | 模块打包工具          |
| SCSS       | -      | CSS 预处理器          |
| Ace Editor | 1.34.x | 代码编辑器            |

### 支持的数据库

- ✅ MySQL / MariaDB
- ✅ PostgreSQL
- ✅ SQLite
- ✅ Firebird SQL
- 🔜 DuckDB（计划中）
- 🔜 SQL Server（计划中）

---

## 📁 目录结构总览

```
antares/
├── src/                    # 源代码目录
│   ├── common/             # 主进程与渲染进程共享的代码
│   ├── main/               # Electron 主进程代码
│   └── renderer/           # Vue.js 渲染进程代码
├── assets/                 # 应用图标和资源文件
├── docs/                   # 项目文档
├── scripts/                # 构建和开发脚本
├── tests/                  # 测试文件
├── package.json            # 项目配置和依赖
├── tsconfig.json           # TypeScript 配置
├── webpack.main.config.js  # 主进程 Webpack 配置
├── webpack.renderer.config.js # 渲染进程 Webpack 配置
└── webpack.workers.config.js  # Web Workers 配置
```

---

## 🏗️ 核心架构

Antares 采用 **Electron 多进程架构**，将应用分为三个主要部分：

```
┌─────────────────────────────────────────────────────────────┐
│                      Electron 应用                          │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────┐    IPC 通信    ┌──────────────────────┐  │
│  │   主进程     │◄─────────────►│      渲染进程        │  │
│  │   (main)     │               │     (renderer)       │  │
│  └──────────────┘               └──────────────────────┘  │
│         │                                │                 │
│         ▼                                ▼                 │
│  ┌──────────────┐               ┌──────────────────────┐  │
│  │ 数据库客户端  │               │    Vue.js UI 组件    │  │
│  │ • MySQL      │               │    • TheSettingBar   │  │
│  │ • PostgreSQL │               │    • Workspace       │  │
│  │ • SQLite     │               │    • QueryEditor     │  │
│  │ • Firebird   │               │    • ...             │  │
│  └──────────────┘               └──────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────┐  │
│  │                 共享模块 (common)                    │  │
│  │  • 接口定义  • 工具函数  • 数据类型  • 自定义配置     │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 📂 源码详细结构

### 1. `src/common/` - 共享模块

主进程和渲染进程共用的代码。

```
src/common/
├── customizations/          # 数据库特定的自定义配置
│   ├── defaults.ts          # 默认配置
│   ├── firebird.ts          # Firebird 特定配置
│   ├── mysql.ts             # MySQL 特定配置
│   ├── postgresql.ts        # PostgreSQL 特定配置
│   ├── sqlite.ts            # SQLite 特定配置
│   └── index.ts             # 配置导出入口
│
├── data-types/              # 各数据库的数据类型定义
│   ├── firebird.ts          # Firebird 数据类型
│   ├── mysql.ts             # MySQL 数据类型
│   ├── postgresql.ts        # PostgreSQL 数据类型
│   └── sqlite.ts            # SQLite 数据类型
│
├── interfaces/              # TypeScript 接口定义
│   ├── antares.ts           # 核心应用接口
│   ├── customizations.ts    # 自定义配置接口
│   ├── exporter.ts          # 导出功能接口
│   ├── importer.ts          # 导入功能接口
│   ├── tableApis.ts         # 表操作 API 接口
│   └── workers.ts           # Web Worker 接口
│
├── libs/                    # 共享工具库
│   ├── bufferToBase64.ts    # Buffer 转 Base64
│   ├── encrypter.ts         # 加密解密工具
│   ├── fakerCustom.ts       # Faker 自定义生成器
│   ├── formatBytes.ts       # 字节格式化
│   ├── getArrayDepth.ts     # 获取数组深度
│   ├── hexToBinary.ts       # 十六进制转二进制
│   ├── langDetector.ts      # SQL 语言检测
│   ├── mimeFromHex.ts       # 从十六进制获取 MIME 类型
│   ├── sqlUtils.ts          # SQL 工具函数
│   └── uidGen.ts            # 唯一 ID 生成器
│
├── FakerMethods.ts          # Faker.js 方法配置
├── fieldTypes.ts            # 字段类型定义
└── shortcuts.ts             # 快捷键定义
```

### 2. `src/main/` - 主进程

Electron 主进程代码，负责窗口管理、数据库连接、系统交互。

```
src/main/
├── ipc-handlers/            # IPC 消息处理器
│   ├── application.ts       # 应用级别操作
│   ├── connection.ts        # 数据库连接管理
│   ├── database.ts          # 数据库操作
│   ├── functions.ts         # 函数管理
│   ├── routines.ts          # 存储过程管理
│   ├── schedulers.ts        # 调度器管理
│   ├── schema.ts            # 模式操作
│   ├── tables.ts            # 表操作
│   ├── triggers.ts          # 触发器管理
│   ├── updates.ts           # 应用更新检查
│   ├── users.ts             # 用户管理
│   ├── views.ts             # 视图管理
│   └── index.ts             # 处理器入口
│
├── libs/                    # 主进程工具库
│   ├── clients/             # 数据库客户端实现
│   │   ├── BaseClient.ts    # 客户端基类
│   │   ├── FirebirdSQLClient.ts # Firebird 客户端
│   │   ├── MySQLClient.ts   # MySQL 客户端
│   │   ├── PostgreSQLClient.ts # PostgreSQL 客户端
│   │   └── SQLiteClient.ts  # SQLite 客户端
│   │
│   ├── exporters/           # 数据导出器
│   │   ├── BaseExporter.ts  # 导出器基类
│   │   └── ...              # 各类型导出实现
│   │
│   ├── importers/           # 数据导入器
│   │   └── ...              # 各类型导入实现
│   │
│   ├── parsers/             # SQL 解析器
│   │   └── ...              # SQL 解析实现
│   │
│   ├── misc/                # 其他工具
│   │   └── ...
│   │
│   ├── ClientsFactory.ts    # 客户端工厂类
│   └── ShortcutRegister.ts  # 快捷键注册
│
├── workers/                 # Web Workers
│   └── ...                  # 后台任务处理
│
└── main.ts                  # 主进程入口文件
```

### 3. `src/renderer/` - 渲染进程

Vue.js 前端界面代码。

```
src/renderer/
├── components/              # Vue 组件 (93个)
│   │
│   │  # 基础组件
│   ├── BaseConfirmModal.vue      # 确认对话框
│   ├── BaseContextMenu.vue       # 右键菜单
│   ├── BaseIcon.vue              # 图标组件
│   ├── BaseLoader.vue            # 加载指示器
│   ├── BaseMap.vue               # 地图显示组件
│   ├── BaseNotification.vue      # 通知组件
│   ├── BaseSelect.vue            # 下拉选择框
│   ├── BaseTextEditor.vue        # 文本编辑器
│   ├── BaseUploadInput.vue       # 文件上传
│   ├── BaseVirtualScroll.vue     # 虚拟滚动
│   │
│   │  # 布局组件
│   ├── TheTitleBar.vue           # 标题栏
│   ├── TheSettingBar.vue         # 设置侧边栏
│   ├── TheFooter.vue             # 页脚状态栏
│   ├── TheNotificationsBoard.vue # 通知面板
│   ├── TheScratchpad.vue         # 草稿本
│   │
│   │  # 工作区组件
│   ├── Workspace.vue             # 工作区主容器
│   ├── WorkspaceAddConnectionPanel.vue   # 新建连接面板
│   ├── WorkspaceEditConnectionPanel.vue  # 编辑连接面板
│   ├── WorkspaceEmptyState.vue           # 空状态提示
│   ├── WorkspaceExploreBar.vue           # 数据库导航栏
│   ├── WorkspaceExploreBarSchema.vue     # 模式导航
│   │
│   │  # 查询与数据组件
│   ├── WorkspaceTabQuery.vue            # 查询标签页
│   ├── WorkspaceTabQueryTable.vue       # 查询结果表格
│   ├── WorkspaceTabQueryTableRow.vue    # 结果表格行
│   ├── WorkspaceTabTable.vue            # 数据表标签页
│   ├── QueryEditor.vue                  # SQL 查询编辑器
│   │
│   │  # 数据库对象编辑组件
│   ├── WorkspaceTabNewTable.vue         # 新建表
│   ├── WorkspaceTabNewView.vue          # 新建视图
│   ├── WorkspaceTabNewFunction.vue      # 新建函数
│   ├── WorkspaceTabNewRoutine.vue       # 新建存储过程
│   ├── WorkspaceTabNewTrigger.vue       # 新建触发器
│   ├── WorkspaceTabNewScheduler.vue     # 新建调度器
│   │
│   │  # 对象属性编辑组件
│   ├── WorkspaceTabPropsTable.vue       # 表属性
│   ├── WorkspaceTabPropsView.vue        # 视图属性
│   ├── WorkspaceTabPropsFunction.vue    # 函数属性
│   ├── WorkspaceTabPropsRoutine.vue     # 存储过程属性
│   ├── WorkspaceTabPropsTrigger.vue     # 触发器属性
│   ├── WorkspaceTabPropsScheduler.vue   # 调度器属性
│   │
│   │  # 模态框组件
│   ├── ModalSettings.vue                # 设置对话框
│   ├── ModalAllConnections.vue          # 所有连接
│   ├── ModalExportSchema.vue            # 导出模式
│   ├── ModalImportSchema.vue            # 导入模式
│   ├── ModalFakerRows.vue               # Faker 数据生成
│   ├── ModalHistory.vue                 # 查询历史
│   ├── ModalProcessesList.vue           # 进程列表
│   │
│   └── ...                              # 更多组件
│
├── composables/             # Vue 组合式函数
│   ├── useFilters.ts        # 过滤器逻辑
│   ├── useFocusTrap.ts      # 焦点陷阱
│   └── useResultTables.ts   # 结果表格逻辑
│
├── i18n/                    # 国际化翻译
│   ├── index.ts             # i18n 配置
│   ├── supported-locales.ts # 支持的语言列表
│   ├── en-US.ts             # 英语（美国）
│   ├── zh-CN.ts             # 简体中文
│   ├── zh-TW.ts             # 繁体中文
│   ├── ja-JP.ts             # 日语
│   └── ...                  # 更多语言
│
├── ipc-api/                 # 渲染进程 IPC API
│   ├── Application.ts       # 应用 API
│   ├── Connection.ts        # 连接 API
│   ├── Databases.ts         # 数据库 API
│   ├── Functions.ts         # 函数 API
│   ├── Routines.ts          # 存储过程 API
│   ├── Schedulers.ts        # 调度器 API
│   ├── Schema.ts            # 模式 API
│   ├── Tables.ts            # 表 API
│   ├── Triggers.ts          # 触发器 API
│   ├── Users.ts             # 用户 API
│   └── Views.ts             # 视图 API
│
├── libs/                    # 渲染进程工具库
│   ├── camelize.ts          # 驼峰命名转换
│   ├── colorShade.ts        # 颜色明暗调整
│   ├── copyText.ts          # 复制文本到剪贴板
│   ├── exportRows.ts        # 导出行数据
│   ├── getContrast.ts       # 获取对比色
│   ├── hexToRgba.ts         # 十六进制转 RGBA
│   ├── unproxify.ts         # 解除 Vue 响应式代理
│   └── ext-language_tools.js # Ace 编辑器语言工具扩展
│
├── stores/                  # Pinia 状态管理
│   ├── application.ts       # 应用状态
│   ├── connections.ts       # 连接状态
│   ├── console.ts           # 控制台状态
│   ├── history.ts           # 历史记录状态
│   ├── notifications.ts     # 通知状态
│   ├── schemaExport.ts      # 模式导出状态
│   ├── scratchpad.ts        # 草稿本状态
│   ├── settings.ts          # 设置状态
│   └── workspaces.ts        # 工作区状态
│
├── scss/                    # 样式文件
│   └── ...                  # SCSS 样式
│
├── images/                  # 图片资源
│   └── ...                  # 图标和图片
│
├── App.vue                  # Vue 根组件
├── index.ts                 # 渲染进程入口
├── index.ejs                # HTML 模板
└── untyped.d.ts             # 无类型声明
```

---

## 🔄 数据流与通信

### IPC 通信流程

```
渲染进程 (Vue)                    主进程 (Node.js)
     │                                  │
     │  ipcRenderer.invoke('tables:xxx') │
     ├─────────────────────────────────►│
     │                                  │
     │                      ipc-handlers/tables.ts
     │                                  │
     │                        调用 MySQLClient
     │                                  │
     │◄─────────────────────────────────┤
     │        返回查询结果               │
```

### 状态管理流程

```
Vue 组件
    │
    ▼
Pinia Store (workspaces.ts, connections.ts, ...)
    │
    ▼
IPC API (Tables.ts, Schema.ts, ...)
    │
    ▼
主进程 IPC Handlers
    │
    ▼
数据库客户端 (MySQLClient, PostgreSQLClient, ...)
```

---

## 🚀 开发指南

### 开发环境设置

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run debug

# 代码检查
npm run lint

# 修复代码格式
npm run lint:fix
```

### 构建命令

```bash
# 编译所有模块
npm run compile

# 构建生产版本
npm run build

# 构建 Windows AppX
npm run build:appx
```

### 项目结构关键点

1. **主进程入口**: `src/main/main.ts`
2. **渲染进程入口**: `src/renderer/index.ts`
3. **Vue 根组件**: `src/renderer/App.vue`
4. **路由**: 无传统路由，通过 Pinia Store 管理工作区切换
5. **状态管理**: Pinia stores 位于 `src/renderer/stores/`

### 添加新数据库支持

1. 在 `src/main/libs/clients/` 创建新客户端类，继承 `BaseClient`
2. 在 `src/common/data-types/` 添加数据类型定义
3. 在 `src/common/customizations/` 添加自定义配置
4. 更新 `ClientsFactory.ts` 注册新客户端

### 添加新 UI 组件

1. 在 `src/renderer/components/` 创建 Vue 组件
2. 使用 `<script setup lang="ts">` 语法
3. 遵循现有命名规范（如 `WorkspaceTab*.vue`）
4. 在需要的地方导入使用组件

---

## 📋 文件说明索引

| 文件/目录              | 说明                   |
| ---------------------- | ---------------------- |
| `package.json`         | 项目配置、脚本和依赖   |
| `tsconfig.json`        | TypeScript 编译配置    |
| `webpack.*.config.js`  | Webpack 构建配置       |
| `.eslintrc`            | ESLint 代码检查规则    |
| `.stylelintrc`         | StyleLint CSS 检查规则 |
| `commitlint.config.js` | Git 提交信息规范       |
| `CHANGELOG.md`         | 版本变更日志           |
| `CONTRIBUTING.md`      | 贡献指南               |

---

## 📊 统计信息

- **Vue 组件数量**: 94 个
- **TypeScript 文件**: 117 个
- **支持语言**: 21 种
- **代码行数**: ~50,000+

---

## 📝 代码注释说明

本项目所有 `.vue` 和 `.ts` 源代码文件都已添加详细的中文注释。

### 注释格式

**TypeScript 文件 (.ts)**:
```typescript
/**
 * @file 文件名.ts
 * @description 文件功能描述
 *
 * 详细说明...
 *
 * @module 模块路径
 */
```

**Vue 组件 (.vue)**:
```vue
<!--
  @file 组件名.vue
  @description 组件功能描述

  详细说明...

  @module 模块路径
-->
```

### 注释覆盖统计

| 文件类型        | 文件数量 | 已注释  |
| --------------- | -------- | ------- |
| Vue 组件        | 94       | 94      |
| TypeScript 文件 | 95       | 95      |
| **总计**        | **189**  | **189** |

### 运行注释脚本

如需批量添加或更新注释，可使用以下脚本：

```bash
# 运行注释脚本
node scripts/add-comments.js
```

脚本会自动跳过已有注释的文件，仅为新文件添加注释。

---

## 🔗 相关链接

- [GitHub 仓库](https://github.com/antares-sql/antares)
- [官方网站](https://antares-sql.app/)
- [贡献指南](https://github.com/antares-sql/antares/wiki/Contributors-Guide)
- [翻译指南](https://github.com/antares-sql/antares/wiki/Translate-Antares)

---

*文档生成时间: 2026-02-08*

